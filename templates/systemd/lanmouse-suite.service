[Unit]
Description=Lan Mouse Suite trusted-LAN guard and clipboard bridge
After=network-online.target
Wants=network-online.target
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=@CLI@ service run
Restart=on-failure
RestartSec=3

[Install]
WantedBy=graphical-session.target
